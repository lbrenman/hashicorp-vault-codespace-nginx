error_log /tmp/nginx-error.log warn;
pid /tmp/nginx.pid;

events {
  worker_connections 1024;
}

http {
  access_log /tmp/nginx-access.log;

  # Cache tokens briefly to avoid a login call on every request
  lua_shared_dict token_cache 10m;

  server {
    listen 8100;

    location / {
      access_by_lua_block {
        local cache = ngx.shared.token_cache

        -- Extract Basic Auth header (nginx lowercases all headers)
        local auth_header = ngx.req.get_headers()["authorization"]
        if not auth_header then
          ngx.status = 401
          ngx.header["WWW-Authenticate"] = 'Basic realm="Vault"'
          ngx.say("Missing Authorization header")
          return ngx.exit(401)
        end

        local encoded = auth_header:match("^Basic%s+(.+)$")
        if not encoded then
          ngx.status = 401
          ngx.say("Invalid Authorization header")
          return ngx.exit(401)
        end

        local decoded = ngx.decode_base64(encoded)
        local username, password = decoded:match("^([^:]+):(.+)$")
        if not username or not password then
          ngx.status = 401
          ngx.say("Invalid credentials format")
          return ngx.exit(401)
        end

        -- Check token cache first
        local cached_token = cache:get(encoded)
        if cached_token then
          ngx.req.set_header("X-Vault-Token", cached_token)
          ngx.req.clear_header("authorization")
          return
        end

        -- Exchange credentials for a Vault token via internal subrequest
        -- Uses ngx.location.capture — no external Lua HTTP library needed
        local body = '{"password":"' .. password .. '"}'
        local res = ngx.location.capture(
          "/vault-auth/" .. username,
          { method = ngx.HTTP_POST, body = body }
        )

        if not res or res.status ~= 200 then
          ngx.status = 403
          ngx.say("Vault auth failed: status=" .. (res and res.status or "nil"))
          return ngx.exit(403)
        end

        local cjson = require "cjson"
        local data = cjson.decode(res.body)
        local token = data.auth.client_token

        -- Cache token for 5 minutes
        cache:set(encoded, token, 300)

        -- Swap Authorization header for X-Vault-Token
        ngx.req.set_header("X-Vault-Token", token)
        ngx.req.clear_header("authorization")
      }

      proxy_pass http://127.0.0.1:8200;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
    }

    # Internal location for Vault userpass auth — not accessible externally
    location /vault-auth/ {
      internal;
      proxy_pass http://127.0.0.1:8200/v1/auth/userpass/login/;
      proxy_set_header Content-Type application/json;
    }
  }
}
